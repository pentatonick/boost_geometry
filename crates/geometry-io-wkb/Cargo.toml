[package]
name = "geometry-io-wkb"
version                = "0.0.6"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
license-file.workspace = true
repository.workspace   = true
keywords.workspace     = true
categories.workspace   = true
description = "OGC Well-Known Binary (WKB) reader and writer for the geometry model."
readme = "README.md"

[features]
default = ["std"]
std = [
    "geometry-coords/std",
    "geometry-cs/std",
    "geometry-trait/std",
    "geometry-model/std",
]

[dependencies]
geometry-tag    = { version = "0.0.6", path = "../geometry-tag" }
geometry-coords = { version = "0.0.6", path = "../geometry-coords", default-features = false }
geometry-cs     = { version = "0.0.6", path = "../geometry-cs",     default-features = false }
geometry-trait  = { version = "0.0.6", path = "../geometry-trait",  default-features = false }
geometry-model  = { version = "0.0.6", path = "../geometry-model",  default-features = false }

[lints]
workspace = true
