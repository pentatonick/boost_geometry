# geometry-io-geojson

RFC 7946 GeoJSON reader and writer. `from_geojson` parses into a
`DynGeometry`; `to_geojson` serialises a model geometry. Geometry
members only (no Feature/FeatureCollection property bags).
