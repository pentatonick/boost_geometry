[package]
name = "geometry-adapt-nalgebra"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
license-file.workspace = true
repository.workspace   = true
keywords.workspace     = true
categories.workspace   = true
description = "Adapt nalgebra points and vectors to the geometry-trait concept surface."

[dependencies]
geometry-tag    = { version = "0.0.2", path = "../geometry-tag" }
geometry-coords = { version = "0.0.2", path = "../geometry-coords" }
geometry-cs     = { version = "0.0.2", path = "../geometry-cs" }
geometry-trait  = { version = "0.0.2", path = "../geometry-trait" }
nalgebra        = "0.32"

[dev-dependencies]
geometry-algorithm = { path = "../geometry-algorithm" }

[lints]
workspace = true
