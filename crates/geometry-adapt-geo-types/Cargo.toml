[package]
name = "geometry-adapt-geo-types"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
license-file.workspace = true
repository.workspace   = true
keywords.workspace     = true
categories.workspace   = true
description = "Adapt geo-types geometries to the geometry-trait concept surface."
readme = "README.md"

[dependencies]
geometry-tag    = { workspace = true }
geometry-coords = { workspace = true, features = ["std"] }
geometry-cs     = { workspace = true, features = ["std"] }
geometry-trait  = { workspace = true, features = ["std"] }
geometry-model  = { workspace = true, features = ["std"] }
geo-types       = "0.7"

[dev-dependencies]
geometry-algorithm = { workspace = true }

[lints]
workspace = true
