[package]
name = "geometry-rtree"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
license-file.workspace = true
repository.workspace   = true
description = "R-tree spatial index over the geometry kernel — insert, spatial query, and nearest-neighbour search."
readme = "README.md"

[features]
default = ["std"]
std = []

[dependencies]
geometry-tag       = { version = "0.0.1", path = "../geometry-tag" }
geometry-coords    = { version = "0.0.1", path = "../geometry-coords" }
geometry-cs        = { version = "0.0.1", path = "../geometry-cs" }
geometry-trait     = { version = "0.0.1", path = "../geometry-trait" }
geometry-model     = { version = "0.0.1", path = "../geometry-model" }
geometry-algorithm = { version = "0.0.1", path = "../geometry-algorithm" }

[lints]
workspace = true
