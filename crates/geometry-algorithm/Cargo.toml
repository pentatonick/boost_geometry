[package]
name = "geometry-algorithm"
version                = "0.0.5"
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
license-file.workspace = true
repository.workspace   = true
keywords.workspace     = true
categories.workspace   = true
description = "Free-function algorithms (distance, length, area, within, intersects, …) ported from Boost.Geometry."
readme = "README.md"

[features]
default = ["std"]
std = [
    "geometry-coords/std",
    "geometry-cs/std",
    "geometry-trait/std",
    "geometry-strategy/std",
    "geometry-model/std",
]
libm = [
    "geometry-coords/libm",
    "geometry-trait/libm",
    "geometry-strategy/libm",
    "geometry-model/libm",
]

[dependencies]
geometry-coords   = { version = "0.0.5", path = "../geometry-coords",   default-features = false }
geometry-tag      = { version = "0.0.5", path = "../geometry-tag",      default-features = false }
geometry-cs       = { version = "0.0.5", path = "../geometry-cs",       default-features = false }
geometry-trait    = { version = "0.0.5", path = "../geometry-trait",    default-features = false }
geometry-strategy = { version = "0.0.5", path = "../geometry-strategy", default-features = false }
geometry-model    = { version = "0.0.5", path = "../geometry-model",    default-features = false }

[dev-dependencies]
geometry-adapt    = { path = "../geometry-adapt" }
geometry-coords   = { path = "../geometry-coords" }
geometry-cs       = { path = "../geometry-cs" }
geometry-derive   = { path = "../geometry-derive" }
geometry-model    = { path = "../geometry-model" }
geometry-strategy = { path = "../geometry-strategy" }
geometry-tag      = { path = "../geometry-tag" }
geometry-trait    = { path = "../geometry-trait" }

[lints]
workspace = true
