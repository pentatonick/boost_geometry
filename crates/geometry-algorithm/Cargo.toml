[package]
name = "geometry-algorithm"
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
repository.workspace   = true
description = "Free-function algorithms (distance, length, area, within, intersects, …)."
readme = "README.md"

[features]
default = ["std"]
std = [
    "geometry-coords/std",
    "geometry-cs/std",
    "geometry-trait/std",
    "geometry-strategy/std",
    "geometry-model/std",
]
libm = [
    "geometry-coords/libm",
    "geometry-trait/libm",
    "geometry-strategy/libm",
    "geometry-model/libm",
]

[dependencies]
geometry-coords   = { version = "0.0.1", path = "../geometry-coords",   default-features = false }
geometry-tag      = { version = "0.0.1", path = "../geometry-tag",      default-features = false }
geometry-cs       = { version = "0.0.1", path = "../geometry-cs",       default-features = false }
geometry-trait    = { version = "0.0.1", path = "../geometry-trait",    default-features = false }
geometry-strategy = { version = "0.0.1", path = "../geometry-strategy", default-features = false }
geometry-model    = { version = "0.0.1", path = "../geometry-model",    default-features = false }

[dev-dependencies]
geometry-adapt    = { version = "0.0.1", path = "../geometry-adapt" }
geometry-coords   = { version = "0.0.1", path = "../geometry-coords" }
geometry-cs       = { version = "0.0.1", path = "../geometry-cs" }
geometry-derive   = { version = "0.0.1", path = "../geometry-derive" }
geometry-model    = { version = "0.0.1", path = "../geometry-model" }
geometry-strategy = { version = "0.0.1", path = "../geometry-strategy" }
geometry-tag      = { version = "0.0.1", path = "../geometry-tag" }
geometry-trait    = { version = "0.0.1", path = "../geometry-trait" }

[lints]
workspace = true
