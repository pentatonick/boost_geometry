[package]
name = "geometry-io-ewkt"
version = "0.0.8"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "PostGIS Extended Well-Known Text (EWKT) reader and writer for the geometry model."
readme = "README.md"

[features]
default = ["std"]
std = [
    "geometry-io-wkt/std",
    "geometry-cs/std",
    "geometry-trait/std",
    "geometry-model/std",
]
libm = ["geometry-cs/libm", "geometry-trait/libm", "geometry-model/libm"]

[dependencies]
geometry-io-wkt = { version = "0.0.8", path = "../geometry-io-wkt", default-features = false }
geometry-cs     = { version = "0.0.8", path = "../geometry-cs",     default-features = false }
geometry-trait  = { version = "0.0.8", path = "../geometry-trait",  default-features = false }
geometry-model  = { version = "0.0.8", path = "../geometry-model",  default-features = false }

[dev-dependencies]
geometry-tag = { version = "0.0.7", path = "../geometry-tag" }

[lints]
workspace = true
