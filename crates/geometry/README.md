# geometry

Facade crate re-exporting the geometry workspace as a single API surface.

Re-exports `geometry-tag`, `geometry-coords`, `geometry-cs`, `geometry-trait`,
`geometry-model`, `geometry-strategy`, `geometry-algorithm`, `geometry-adapt`,
and `geometry-derive` under one namespace.
