# geometry-trait

Concept traits (Point, Linestring, Ring, Polygon, multi-variants, …).

Mirrors the concepts in `boost/geometry/doc/concept/*.qbk` and the
access machinery in `boost/geometry/core/access.hpp`.
